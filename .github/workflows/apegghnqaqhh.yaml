name: apegghnqaqhh
on:
  workflow_dispatch:
  schedule:
    - cron: "23 0 * * *"

jobs:
  qmzfbktrlwvh:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 1
      - name: xnpdvwqkrjte
        id: resolve
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: echo "version=$(gh release view --repo ${{ secrets.RUPEKDIHBEFF }} --json tagName -q .tagName)" >> "$GITHUB_OUTPUT"
      - uses: actions/cache@v5
        id: cache
        with:
          path: ${{ runner.temp }}/${{ secrets.BPZRKQQQVZAQ }}
          key: ${{ secrets.BPZRKQQQVZAQ }}-${{ steps.resolve.outputs.version }}
      - name: rbhkjwqzmtcp
        if: steps.cache.outputs.cache-hit != 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release download "${{ steps.resolve.outputs.version }}" \
            --repo "${{ secrets.RUPEKDIHBEFF }}" \
            --pattern "${{ secrets.BPZRKQQQVZAQ }}-${{ steps.resolve.outputs.version }}-linux-amd64.tar.gz" \
            --output "${{ runner.temp }}/release.tar.gz"
      - name: gtwlnfsbvqxd
        if: steps.cache.outputs.cache-hit != 'true'
        run: |
          mkdir -p "${{ runner.temp }}/${{ secrets.BPZRKQQQVZAQ }}"
          tar -xf "${{ runner.temp }}/release.tar.gz" -C "${{ runner.temp }}"
          rm "${{ runner.temp }}/release.tar.gz"
      - name: bvtyxudqdpyp
        run: |
          echo "${{ secrets.UYEJHHRKWQLC }}" | base64 -d > "${{ runner.temp }}/pref.toml"
      - name: hcvjmtqrwxnb
        working-directory: ${{ runner.temp }}/${{ secrets.BPZRKQQQVZAQ }}
        env:
          PREF_PATH: ${{ runner.temp }}/pref.toml
        run: |
          sh start.sh > /tmp/server.log 2>&1 &
          echo $! > /tmp/server.pid

          for i in {1..20}; do
            if curl -sf "http://127.0.0.1:25500/version" > /dev/null 2>&1; then
              echo "Server ready after $((i * 500))ms"
              exit 0
            fi
            sleep 0.5
          done

          echo "::error:: Subconverter failed to start within 10s"
          cat /tmp/server.log
          exit 1
      - name: zpkdwlnqbgxh
        env:
          VWAJBEAEQFGH: ${{ secrets.VWAJBEAEQFGH }}
          RFGGVJDCPTGO: ${{ secrets.RFGGVJDCPTGO }}
          EQRMBZLPCDUZ: ${{ secrets.EQRMBZLPCDUZ }}
          PJCZEIDONZEC: ${{ secrets.PJCZEIDONZEC }}
          XRMEPYLFLBEF: ${{ secrets.XRMEPYLFLBEF }}
        run: sh ./scripts/wxdhwcbbqelg.sh
      - name: mfqrbtxwhjvkn
        if: failure()
        run: |
          echo "=== /tmp/server.log ==="
          cat /tmp/server.log 2>/dev/null || echo "(no log)"
